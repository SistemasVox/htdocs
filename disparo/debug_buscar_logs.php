<?php
ini_set("display_errors", 1);
ini_set("display_startup_errors", 1);
error_reporting(E_ALL);

require_once 'buscar_logs.php';

/* // Número de telefone a ser consultado
$numero = '34991509519';

// Chame a função consultarWhatsApp
$temWhatsApp = consultarWhatsApp($numero);

// Verifique o resultado
if ($temWhatsApp) {
    echo "O número de telefone $numero tem WhatsApp.". "<br>";
} else {
    echo "O número de telefone $numero não tem WhatsApp.". ".<br>";
} */

/* echo getDataServidor($conn). ".<br>";
echo json_decode(getDataServidor($conn), true)['message']. ".<br>";
echo json_decode(getDataServidor($conn))->message. ".<br>"; */

/* echo getServicos($conn); */

/* // Definir os valores de entrada
$dataInicio = '2023-07-18';
$horaInicio = '09:00';
$horaFim = '17:00';

// Chamar a função para obter a próxima data e hora disponível
$proximaDataHora = gerarProximaDataHoraDisponivel($dataInicio, $horaInicio, $horaFim);

// Imprimir a próxima data e hora disponível
echo "Próxima data e hora disponível: " . $proximaDataHora; */

?>